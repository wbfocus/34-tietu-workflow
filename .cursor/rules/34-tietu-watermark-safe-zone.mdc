---
description: 3:4贴图底部水印安全区 + 画布高度防裁切（1080×1440 铁律）
alwaysApply: true
---

# 3:4 贴图 · 水印安全区 & 防底边裁切

写 / 改 `HTMLcards` 或走 `34-tietu-workflow` 时：

## A. 水印安全区（之前已修，继续执行）

1. 最外层 `1080×1440` 容器用 **`pt-9 pb-20`**（`pb-20` 写在这一层，不要只写内层子 div）。
2. **默认不要**在水印行高度内安排正文、提醒框、CTA。
3. 内容偏满时：先收紧间距/字号/块内边距，不要往下挤穿底边。
4. 出图前自检：水印整行可读、不被遮挡。

## B. 画布总高度防裁切（060 / 099 教训，必须同时检查）

固定画布 `h-[1440px]` + `overflow-hidden` 时，**内容总高度超过 1440px 会被直接裁掉底部**——表现是金句/提醒框下半截消失，不是水印叠字。

写 HTML 时 **禁止**：

- 内层内容区用 **`flex-1`** 后再堆多块卡片（mesh + 标题 + 数字行 + 多卡 + featured + 独立底栏极易超高）
- 为「饱满」连续加独立 CTA / 提醒条 / 自检栏而不做高度预算

内容偏多时 **必须**（按顺序）：

1. 不要加 Mesh 顶区占位（Mesh 只做全卡氛围层）
2. 标题与冲击数字 **并排**，不要各占一整行
3. 收紧 `gap` / 卡片 `p-*`
4. **合并**金句 + 自检进同一张 `.card-principle`，去掉独立底栏
5. 仍满则删一块或拆第二张，**不要**指望 `pb-20` 能救裁切

## C. 截图前必做（Agent 不可跳过）

- [ ] `pb-20` 在最外层 `w-[1080px] h-[1440px]` div
- [ ] 内层内容区无 `flex-1` 撑高（流程图 skill 横向区除外）
- [ ] 心算：页眉 + 标题区 + N 张卡 + `.c-foot` ≤ 约 **1320px**（留 80px 水印区 + 余量）
- [ ] 最后一块内容（原则框 / `.c-foot`）**完整可见**，底部有空白再是水印
- [ ] **底 25% 不能只有水印**：内容偏少时用 `.board` / `.board-cover`（均为约 **1310px**）+ `justify-content:space-between` 拉开块间距或加大字号，把最后一块贴到水印安全区上沿；**禁止 `flex-1`**

## D. 竖向铺满（底部禁止空一大块米色）

水印安全区（A）防的是「压字」，防裁切（B）防的是「超高被切」。还要防第三种：**内容全堆在上半截，底下空 25%–40% 米色**。

写 HTML 时 **必须**：

1. 内页内容外包 `.board`：`display:flex; flex-direction:column; justify-content:space-between; height:1310px`（写进该套图 `theme-*.css`，**CSS 链接不要带 `?v=` 查询串**，Chrome `file://` 截图会加载失败）
2. 封面/结尾用 `.board-cover`，高度同样约 `1310px`（Mesh/光晕只做全卡氛围，**禁止**再切 200–300px 顶区）
3. 块与块空隙拉开；仍空则加大标题/正文字号和卡片 `p-*`，不要加空洞底栏
4. 截图后目视：最后一块（`.c-foot` / 原则框）贴在水印上沿，不是漂在画面中部

可用脚本自检：

```bash
py -3 ".cursor/skills/34-tietu-workflow/scripts/qa_tietu.py" pngs "<成品图文件夹>"
```
